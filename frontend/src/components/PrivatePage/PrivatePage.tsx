import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom'; // Added import for useNavigate

function PrivatePage() {

  return (
    <div >
      <h1>This is where all the secret stuff happens</h1>
      
    </div>
  );
}

export default PrivatePage;
